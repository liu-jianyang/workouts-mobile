import * as React from 'react';
import {
    StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row',
    },
    icon: {
        width: 20,
        height: 20,
        resizeMode: 'contain',
    },
});